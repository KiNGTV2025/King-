name: Update Sözcü TV M3U8

on:
  schedule:
    - cron: "*/10 * * * *"  # her 10 dakikada bir
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Repo indir
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Python yükle
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: yt-dlp yükle
        run: pip install -U yt-dlp

      - name: Playlist oluştur
        run: python3 update_playlist.py

      - name: Değişiklikleri kaydet ve gönder
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "actions@github.com"

          # 🔹 Değişiklik varsa commit et
          git add playlist/Sozcu_Tv.m3u8
          git commit -m "Auto update M3U8 [$(date)]" || echo "No changes to commit"

          # 🔹 Uzaktaki değişiklikleri al (önce commit ettik)
          git pull --rebase origin main || true

          # 🔹 Push et
          git push origin main || true
